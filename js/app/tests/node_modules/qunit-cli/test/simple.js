QUnit.module(document);

test('simple test', function(assert) {
	assert.expect(1);

	var elem = assert.document.createElement("div");

	elem.style.position = "absolute";

	assert.equals(elem.style.position, "absolute");
});